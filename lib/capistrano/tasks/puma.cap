namespace :puma do

  desc "Restart puma webserver."
  task :restart do
    on roles(:app) do
      within current_path do
        execute "cd #{current_path}; bin/puma.sh restart"
      end
    end
  end
end
