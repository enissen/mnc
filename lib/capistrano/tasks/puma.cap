namespace :puma do

  desc "Restart puma webserver."
  task :restart do
    on roles(:app) do
      within release_path do
        execute "cd #{release_path}; bin/puma.sh restart"
      end
    end
  end
end
