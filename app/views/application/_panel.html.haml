.user_nav
  - if user_signed_in?
    #{current_user.email} | #{link_to "Logout", logout_path, method: :delete }
  - else
    = link_to "Login", login_path, class: "login"


.lang_nav
  = link_to "en", locale: "en" # url_for("#{spot_on(request.original_url, :en)}?locale=en") #, onclick: "window.location.reload();"
  = link_to "de", locale: "de" #spot_on(request.original_url, :de)
  = link_to "es", locale: "es"# , spot_on(request.original_url, :es)